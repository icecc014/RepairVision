syntax = "proto3";

package worker;
option go_package="./worker";

message Request {
  string ping = 1;
}

message Response {
  string pong = 1;
}

service Worker {
  rpc Ping(Request) returns(Response);
}
