# 06_V3规划与开工准备.md（2026-09-09 新增）

> V3 在已推送 GitHub 的 main（V1+V2 完成态，commit 7617529）基础上开发。
> 开发分支：feature/v3。本文件随 V3 滚动更新。

## 1. V3 目标

把“单发加权派单”升级为“字段完备的约束派单 + 轮休排班 + 可人工干预”，
同时补齐 4 栋演示数据、房间合法性校验、管理端调度工作台与看板、H5 详情与分页，
为论文提供可对比的调度算法实验与完整演示链路。

本阶段按 01/03 约束执行：
- 不新增白名单外依赖（批量指派算法手写 Hungarian/min-cost，不引 OR-Tools/Timefold 等库）。
- 所有 DB 变更先提交申请、备份后执行；本文件即变更申请载体。
- 每次可运行改动 `go build ./...`、两端 `npm run build` 全绿后再提交。

## 2. V3 阶段分布

| 阶段 | 目标 | 主要交付 |
| --- | --- | --- |
| V3.0 领域底座 | 调度所需数据就绪 | 4 栋楼与 dorm4、更多工人种子；orders 加 priority/expect_minutes/时间点；users 加 max_concurrent；worker_schedules 表；房间合法性服务端校验；V3 规划文档；迁移与备份 |
| V3.1 派单算法升级 | 单发贪心 → 约束 + 可批量 + 可改派 | 独立派单引擎；单发/批量/人工改派三入口；优先级/预计工时/最大并发/当班过滤；dispatch_records 状态语义 1/2/3 补齐；PC 派单工作台 |
| V3.2 轮休排班 | 建立“工人作息”闭环 | 周排班模板生成与覆盖；管理员排班日历；派单只派当班工人；工人端“我的班次” |
| V3.3 端到端 UI | 三端页面与功能同步 | PC 工单详情抽屉/调度看板；宿管详情页与联系工人；工人今日任务；列表分页 |
| V3.4 SLA/统计 | 监督闭环 | 超时规则与看板；响应/维修时长统计；待派滞留原因 |
| V3.5 收尾加固 | 答辩级质量 | 单元测试；演示数据脚本；AGENTS/README/论文素材；合并 main 推送 GitHub |

## 3. V3.0 DDL 变更申请（待人工确认后执行）

> 申请时间 2026-09-09；迁移脚本：db/migrations/20260909_v3_0.sql（手动迁移），新装环境已并入 db/init/01_schema.sql。
> 备份：db/backups/pre_v3_20260909.sql（已 gitignore）。执行状态：✅ 2026-09-09 已执行。

### order_db.orders（ALTER，兼容旧数据）

```sql
ALTER TABLE orders
  ADD COLUMN priority TINYINT NOT NULL DEFAULT 1 COMMENT '1普通 2紧急' AFTER fault_type,
  ADD COLUMN expect_minutes INT NOT NULL DEFAULT 30 COMMENT '预计维修时长(分钟)' AFTER priority,
  ADD COLUMN dispatched_at DATETIME DEFAULT NULL COMMENT '派单时间' AFTER worker_id,
  ADD COLUMN started_at DATETIME DEFAULT NULL COMMENT '工人开工时间' AFTER dispatched_at,
  ADD COLUMN completed_at DATETIME DEFAULT NULL COMMENT '完成时间' AFTER started_at;
```


### worker_db.users（ALTER）

```sql
ALTER TABLE users
  ADD COLUMN max_concurrent TINYINT NOT NULL DEFAULT 3 COMMENT '最大在途工单数' AFTER status;
```

### worker_db.worker_schedules（新增表，V3.2 排班用）

```sql
CREATE TABLE IF NOT EXISTS worker_schedules (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  worker_id BIGINT NOT NULL,
  work_date DATE NOT NULL,
  shift_type VARCHAR(20) NOT NULL DEFAULT 'DAY'
    COMMENT 'DAY全天班 MORNING午班 AFTERNOON晚班 OFF休息',
  note VARCHAR(100),
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_worker_date (worker_id, work_date),
  KEY idx_date (work_date)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### map_db（无表结构变更）

仅种子新增第 4 栋测试楼（B2/4号宿舍楼），由 map-rpc 幂等补种。

## 4. V3.0 代码改动清单

### worker 服务
- users 表结构体新增 MaxConcurrent；Insert/Update/查询贯通。
- 种子扩至 6 账号：admin、dorm1-4、worker1-4；worker 覆盖 4 栋楼并提供差异化技能。

### map 服务
- 种子新增第 4 栋楼：B2 4号宿舍楼，6 层、每层 16 间、RoomsPerFloor=16。

### order 服务
- 建单房间合法性校验：楼层 ≤ 楼栋层数、房间号后缀 ≤ 楼栋每层房间数。
- orders 结构体/CRUD 贯通 priority/expect_minutes/dispatched_at/started_at/completed_at；
  状态流转时写对应时间点（派单/开工/完工/取消）。
- 工单响应/建单请求透出 priority 等字段（order.api 类型同步）。

## 5. 验收与回归

- 4 栋楼均可在宿管端报修；非法房间（超出楼层/超出房间区间）被拦截并提示。
- 新订单默认 priority=1、expect_minutes=默认值；派单/开工/完工后时间点落库。
- worker_schedules 表可写入读取（V3.2 前通过 SQL/RPC 校验即可）。
- 三服务 `go build ./...`、两端 `npm run build` 通过；Docker 一键启动与演示闭环无回归。

## V3.0 完成记录（2026-09-09）

- [x] feature/v3 分支、AGENTS/06 规划文档、三库备份（db/backups/pre_v3_20260909.sql）。
- [x] V3.0 DDL 已执行：orders 新列、users.max_concurrent、worker_schedules 表；01_schema.sql 同步（新装直接生效）。
- [x] map 种子新增第 4 栋楼 B2；worker 种子新增 dorm4/worker3/worker4 及管辖与技能。
- [x] order 服务房间合法性校验（超楼层/超房间区间拦截），回归已通过。
- [x] orders 状态流转写入派单/开工/完成时间点；新订单默认 priority=1、expect_minutes=30。
- [x] 三服务 go build + docker 重建启动正常；admin/dorm/worker 登录与列表回归通过。
- [ ] V3.1 派单算法升级（下一步）。
## V3.1a 完成记录（2026-09-09）

- [x] 派单打分抽为独立引擎（dispatch_engine.go）：当班(OFF)过滤、最大并发容量过滤、按预计工时分钟打负载分。
- [x] worker RPC 新增 max_concurrent 与按日期返回当班字段；worker 建号/改号可维护最大并发。
- [x] 新增管理员改派接口 POST /api/admin/orders/:id/reassign；改派写历史链（旧记录 status=3、新记录 status=1），不允许派给非当班/满载工人。
- [x] 工人开工时派单记录置为 status=2（已接受）；orders 时间点/工单状态写入已回归。
- [ ] V3.1b 批量派单（最小代价指派）+ PC 派单工作台（待办/改派操作）未开始。
## V3.1b 完成记录（2026-09-09）

- [x] 新增批量派单接口 POST /api/admin/orders/batch-dispatch：支持全量/楼栋/指定工单三种范围。
- [x] 批量算法：工人按最大并发拆“容量槽位”，对跨楼栋待派单做 Hungarian 最小代价指派；无可用槽位工单保留待派。
- [x] Hungarian 单元测试通过（3x3 与容量槽位两个用例）。
- [x] 管理端用户接口/页面支持 max_concurrent（最大并发）读写。
- [x] PC 工单总览新增：批量派单按钮、待派“手动派单”、已派“改派”与工人选择弹窗。
- [x] 端到端验证：关闭自动派单生成 3 条跨楼栋待派单 → 批量派单全部成功并显示容量约束 → 清理并恢复自动派单。
## V3.2 完成记录（2026-09-09）

- [x] worker RPC 新增 ListSchedules / SaveSchedules / GenerateWeekly；班次类型 DAY/MORNING/AFTERNOON/OFF。
- [x] 一键周排班：每名工人每周休 1 天，轮休日按工人顺序 + 周序号错开；同周可覆盖重生成。
- [x] 派单链路已验证“休息日不派单”：今日全员 OFF 时新单停留在待派，批量派单 remained=1；恢复排班后正常。
- [x] PC 新增“工人排班”页：周一至周日表格、点击单格改班次、上一周/下一周、一键生成本周。
- [x] H5 工人端新增“我的班次”标签，展示本周每日班次与轮休。
- [x] 角色隔离：管理员访问工人排班接口返回 403；worker1 只能看到自己 7 条排班。
- [x] 排班数据当前已生成并保留一周演示数据（2026-09-07 ~ 2026-09-13）。
## V3.3a 完成记录（2026-09-09）

- [x] 工单响应新增 workerPhone；演示工人已补充手机号（13800000001-04），宿管详情可一键拨号联系工人。
- [x] H5 宿管端新增工单详情弹层：完整展示位置/类型/状态/描述/处理工人，并支持拨打工人电话。
- [x] H5 工人端新增“今日任务”筛选胶囊。
- [x] PC 工单总览新增详情抽屉（含工人电话与派单评分）。
- [x] PC 新增“调度看板”页：每名工人当天班次、可派状态、在途/最大并发进度、今日完成数。
- [ ] V3.3b 列表分页统一（PC/H5 工单列表）、待派滞留原因等（未开始）。