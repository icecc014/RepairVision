# 04_工作日志.md（每日工作记录）

## 2026-09-07（Day 1）

### 目标
把“校园维修工单智能调度与2D/3D混合可视化系统”毕设从理想规格转成可执行的 MVP 路线，并完成 P0 工程骨架。

### 已完成
1. 通读并修正 AGENTS 文档
   - 功能数：22 → 19（F01–F19）
   - 容器拓扑：8 容器（1个MySQL实例承载3库 + Kafka KRaft 单节点）
   - 补充依赖白名单：Vite 5.4.11、@vitejs/plugin-vue 5.1.4、vue-router 4.4.5、axios 1.7.7、pinia 2.2.3、vue-konva 3.2.0、go-queue 1.2.2
   - 修正 orders.is_merged/main_order_id 语义，补充 floor
   - 新增 fault_types / dispatch_rule_config / operation_logs 表
   - 管理员密码改为初始化时生成 bcrypt
   - 权限规则改为“RPC取ID集合 + DAO层显式隔离”
2. 确认四项设计决策
   - A：单 MySQL 实例 3 schema
   - B：F14 扩展为“开工+完工”（2→3→4）
   - C：MVP 含 PC 只读工单总览（不编号）
   - D：Kafka 仅 P4 用于 F05/F19
3. 完成 P0 骨架（验收通过）
   - go-zero 三服务：order-api(8888) / worker-rpc(9001) / map-rpc(9002)，均已编译通过
   - 依赖锁定：go-zero v1.7.2 + grpc v1.65.0 + protobuf v1.34.2
   - 双前端 Vite 脚手架可构建：pc-admin(/admin/) + mobile-web(/m/)
   - docker-compose 8 容器一键启动成功
   - 链路验证：`GET /ping/p0` → `{"worker":"worker-rpc:p0","map":"map-rpc:p0"}`
   - MySQL 初始化成功：order_db 6表、worker_db 4表、map_db 2表
4. 提交记录
   - 9d6ea40 docs: 修订毕设规格并新增MVP开发路线
   - ded7df0 docs(AGENTS): 修正goctl验证命令与目录结构
   - 4348438 chore(scaffold): 搭建P0工程骨架
   - 3b19350 fix(scaffold): 打通P0服务链路并记录验收进度
   （本日志提交后还会有本次记录提交）

### 过程中踩过的坑（明天避免）
- `apache/kafka:3.6.1` 官方镜像不存在 → 使用 `confluentinc/cp-kafka:7.6.1`（内核 Kafka 3.6.1）
- cp-kafka 的 CLUSTER_ID 必须是合法 Base64 URL UUID（当前值：om3mBWuen0Cw-UtegkPqUg）
- go-zero v1.7.2 不自动展开 YAML 里的 `${ETCD_ENDPOINTS}` → 容器内 yaml 直接写 `etcd:2379`
- map-rpc 生成包名为 `_map`，改代码时别用 `map.Response`
- goctl 版本命令是 `goctl -v`，不是 `goctl version`
- protoc 23.1 已下载到仓库 `.tools/protoc`（gitignored）；goctl rpc 生成需要它
- 本机 Docker Hub 网络不稳定，镜像已全部缓存，明天无需重拉

### 环境/运行状态
- Docker Desktop 已启动，8 容器当前运行中
- 本地 `.env` 由 `.env.example` 复制生成，MySQL 密码仍为占位值 `change-me-on-first-run`
- 访问入口：http://localhost:8080/admin/ 、 http://localhost:8080/m/ 、 http://localhost:8080/ping/p0

### 明天计划（P1 MVP 最小业务闭环）
1. 种子数据（正式 bcrypt）：admin / 宿管 / 工人，楼栋与维修类型字典
2. JWT 登录 + 角色中间件（order-api 唯一 HTTP 入口）
3. 宿管端：F07 本栋工单列表/详情、F08 极简报修创建、F09 未接单取消
4. 简单自动派单：管辖楼栋内在途最少者，写入 dispatch_records
5. 工人端：我的工单列表（F13基础形态）、F14 开工+完工
6. PC 管理端只读工单总览
7. 完成后提交并更新 03 阶段进度

## 2026-09-08（Day 2）

### 目标
开始 P1 MVP 最小业务闭环：宿管报修 → 自动派单 → 工人开工/完工 → 宿管端状态闭环。

### 已完成（文档/决策阶段）
1. 复核项目：确认仓库为新开项目、P0 已完成、P1 未开始，工作区干净。
2. 经人工确认启动 P1，新增/转正三组依赖并写入白名单：
   - github.com/go-sql-driver/mysql v1.8.1
   - golang.org/x/crypto v0.26.0（bcrypt）
   - github.com/golang-jwt/jwt/v4 v4.5.0（间接转直接）
3. 记录 P1 设计决策 E-H：服务启动幂等种子、创建即自动派单、root 连单实例、conf.UseEnv。
4. 更新 03 阶段进度：P1 标记为“开工、进行中”。

### 当前进度
- [ ] P1 后端：worker/map RPC + order-api 业务接口
- [ ] P1 前端：H5 宿管/工人 + PC 只读总览
- [ ] P1 验收：一键启动 + 全链路演示脚本

### P1 完成记录（2026-09-08 晚些更新）
- 后端：worker-rpc 登录/按楼栋取工人/批量取用户 + map-rpc 楼栋列表 + order-api 登录JWT/角色中间件/宿管与工人工单接口/创建即自动派单。
- 前端：移动端宿管报修+列表+取消、工人工单+开工+完工；PC 管理员登录与只读工单总览。
- 部署：docker-compose 注入 MYSQL_ROOT_PASSWORD/JWT_SECRET，三服务依赖 mysql healthy。
- 验收通过（本机 8080 经 nginx）：
  1. 宿管 dorm1 登录 → 建单 → 自动派给 worker1；
  2. worker1 登录可见已派单 → 开工 → 完工；
  3. dorm1 列表状态变为“已完成”；
  4. admin 总览可见 1 条已完成工单；
  5. 楼栋2连续两单分别派给 worker1/worker2（负载均衡）；
  6. 开工前取消成功，状态“已取消”；
  7. /admin/、/m/、/ping/p0 均 200。
- 修复：worker ListWorkersByBuilding SQL building_id 列歧义（join 改子查询）。

### UI 设计主线启动（2026-09-08）
- 决策：UI 不等待全部 MVP 功能完成，改为 P2 并行主线（设计系统先行、现有页先美化、P2 新页随做随美）。
- 已完成 P2-UI-1/P2-UI-2：
  - 移动端：全局主题变量与 Vant 主题覆盖、登录页品牌视觉、宿管工作台（统计卡+筛选胶囊+工单卡片+极简报修弹层）、工人工单台（统计+状态筛选+开工/完工）。
  - PC 端：Element Plus 主题覆盖、分栏品牌登录页、深色侧边栏布局、统计卡片、筛选面板与美化后的工单表格。
  - 两端 `npm run build` 均通过。
- 提交：`feat(ui): P1三端页面设计系统与界面美化`

### P2 第一批完成（F03/F04，2026-09-08）
- 后端：维修类型管理（列表/新增/编辑/停用启用）、派单规则管理（列表/新增/编辑）、默认规则种子（skill/distance/load/auto_dispatch）。
- PC：新增可复用 AdminShell 布局；工单总览改由布局承载；新增“维修类型字典”“派单规则配置”页面。
- 验收：接口增删改全部通过；`npm run build` 通过；容器已发布。

### P2 第二批完成（F01/F02，2026-09-08）
- worker-rpc：用户列表/新增/编辑/重置密码/停用（启用通过更新状态），工人工号可维护管辖楼栋。
- map-rpc：楼栋新增/编辑/删除/详情；order-api 管理接口转发并做删除保护（已有工单的楼栋不可删）。
- PC：新增“人员账号管理”“建筑信息管理”页面，AdminShell 菜单启用。
- 验收：新增 dorm3 可登录；账号停用/启用、楼栋增改删全部通过；容器已发布。

### P2 第三批完成（F16/F17，2026-09-08）
- F17：worker 技能种子（电/水维修+熟练度）与常驻楼栋；创建工单按 skill/distance/load 三权重加权选人，派单记录写入三维分。
- F16：同楼栋/楼层/房间/维修类型 2 小时内存在未关闭工单时阻止重复报修（演示语义：阻止而非自动合并）。
- 验收：水维修（2号楼）优先派给技能更高的王工；同一房间重复提交被拦截。

### P2 第四批完成（F05/F18，2026-09-08）
- F05：order-api 全局同步操作日志（匿名登录/用户身份/方法路径/响应码/IP/耗时，密码脱敏），PC 新增操作日志页与分页筛选。
- F18：go-zero rest 不支持 Hijacker，故 order-api 进程内新增独立 HTTP 8890 端口承载 WebSocket，nginx `/ws` 转发；宿管/工人/管理员按楼栋/工人/全局订阅，建单/取消/开工/完工实时推送。
- 验收：日志查询正常；WebSocket 握手 101，建单实时收到 order_changed 事件。

### P2 收尾完成（2026-09-08）
- PC 工单总览新增“派单评分”列，悬停展示技能/距离/负载三维分。
- P2 完整回归通过：管理端 6 账号/3 楼栋/4 类型/4 规则；加权派单分配李工并记录评分；同房间重复被拦截；开工/完工闭环；取消成功；操作日志 30+ 条；WebSocket 实时推送；/admin/ /m/ /ping 200。
- 03 标记 P2 完成。

### P3 第一批完成（2026-09-08）
- worker-rpc 增加“我的管辖楼栋”；order-api 增加 `/api/worker/map-data` 与 `/api/worker/batch-complete`。
- 移动端工人端新增“报修地图”：Konva 2D 楼栋总览、红点徽标计数、点击楼栋钻取按类型分组工单、批量完工按钮、Three.js 动态 3D 楼宇（低端设备降级为文本）。
- 验收：worker1 map-data 返回 2 栋楼 5 条活动工单；批量完工返回 count=1。

### P3 收尾完成（2026-09-08）
- map-rpc 增加 UpsertFaultMarker/RemoveFaultMarker；建单写 marker、完工/取消/批量完工删 marker。
- 验收：map-rpc 日志确认 order 11 建单 insert、完工 delete；map-data 返回 2 栋 5 单；批量完工 count=1；两端构建与 /m /admin 200。
- 03 标记 P3 完成。

### P4 第一批完成（F06，2026-09-08）
- order-api 新增 /api/admin/stats：状态汇总、楼栋分布、类型分布、近7日趋势。
- PC 新增“数据统计看板”页（统计卡片 + CSS 条形图，无新增图表库）。
- 验收：5 状态卡、2 楼栋、3 类型、7 天趋势正常；状态分布 已派5/完成4/取消2。

### P4 第二批完成（F19，2026-09-08）
- order-api 接入 go-queue(kq) 生产者，操作日志发送 topic `operation-logs`；进程内 Kafka 消费者异步落库；Kafka 推送失败时回退同步写 operation_logs。
- 验收：登录后日志不再同步出现，数秒后由消费者落库（总量 63→66），topic 已自动创建。

### 功能测试修复（2026-09-08）
- PC 管理端所有接口 401：http 拦截器读 rv_admin_token，而登录态存在 rv_admin_auth，已统一读取并双写 token。
- 移动端工人页空白：WorkerView 使用 onUnmounted 未导入，运行时抛错；已补导入。
- 移动端 401 兜底：自动清理登录态并回 /m/login。

### 前端列表结构修复（2026-09-08）
- 管理端页面报 t.value.filter is not a function：列表接口返回 {list:[...]}，前端却整体当数组使用；已统一在 API 层解包 list（工单/用户/楼栋/类型/规则/宿管工单/工人工单/维修类型）。
- 该问题同时是宿管“极简报修”无类型、按钮不可用的一处根因；新版已发布。

### 工人页布局修复（2026-09-08）
- 模式选项卡与统计卡重叠：取消工人页 rv-content 负边距并收紧头部留白。
- 地图画布宽度依赖 window.innerWidth 可能为 0：加 300px 下限兜底；请刷新后在“报修地图”选项卡查看 2D 总图。

### 移动端角色隔离与报修单优化（2026-09-08）
- 登录态改用 sessionStorage（标签页隔离），路由拆分为 /m/dorm 与 /m/worker，刷新不再互相串角色。
- 宿管报修只填房间号（如 401），楼层由首位数字自动推导；后端同样支持不传 floor 时推导并校验。

### 推送 GitHub（2026-09-08）
- 创建 README，本地分支改为 main，配置远程 origin=https://github.com/icecc014/RepairVision.git。
- 使用本机代理 127.0.0.1:7897 推送成功：main -> main。