# 24_V9.7 管理端移动自适应任务书

> 归属：**V9 阶梯**（2026-09-20 用户确认并入 V9）
> 目标：让 PC 管理端在手机/平板上可用，同时**保证 ≥1024px 宽屏布局零回归**
> 相关：`21_V9统一入口与内网穿透任务书.md`、`22_交接说明书（V9进行中）.md`、`23_V9.5演示与答辩材料.md`

---

## 1. 为什么做（改造前实测）

- `frontend/pc-admin/src/components/AdminShell.vue` 为 `grid-template-columns: 232px 1fr` + `height: 100vh; overflow: hidden`，13 个导航项常驻左侧；390px 手机上侧栏占约 60% 宽度，内容区只剩约 158px。
- 全管理端仅 `views/LoginView.vue` 有一处 `@media (max-width: 860px)`，其余 13 个页面无任何自适应处理。
- 依赖锁死 Element Plus 2.7.5，项目红线禁止新增依赖 → 只能用现有组件 + CSS 媒体查询。

## 2. 断点与原则

| 档位 | 宽度 | 布局 |
| :--- | :--- | :--- |
| 移动 | ≤767px | 侧栏转为抽屉；顶栏压缩；内容单列 |
| 平板 | 768–1023px | 暂保持现状（后续可选优化） |
| 宽屏 | ≥1024px | **与改造前完全一致（回归基线）** |

原则：不新增依赖、不动主题令牌、不改后端接口、宽屏零回归。

## 3. 三批交付

| 阶段 | 内容 | 状态 |
| :--- | :--- | :---: |
| **V9.7.1 外壳** | 抽屉导航 + 汉堡开关 + 遮罩 + 顶栏压缩 + `100dvh` 视口修正 + 安全区留白 | ✅ 已完成 |
| **V9.7.2 高频页卡片化** | 四页全部完成：工单总览 / 请假审批 / 报修记录 / 人员账号 | ✅ 已完成 |
| **V9.7.3 表单与看板** | 全局弹窗/抽屉/表单适配 + 看板栅格降级 + 规则页单列 ✅（本批）；逐页栅格核对 ⬜ 待做 | 🔶 进行中 |
| **可视化（同批）** | 区域概览 / 3D / 楼层设计器在移动端**只读浏览**，编辑入口隐藏并提示「建议在电脑端编辑」 | ⬜ 待做 |

## 4. V9.7.1 实施记录（2026-09-20）

改动文件：`frontend/pc-admin/src/components/AdminShell.vue`（+143 / −4）

- 侧栏加 `:class="{ 'mobile-open': navOpen }"`：≤767px 时 `position: fixed` + `translateX(-100%)`，展开动画 `cubic-bezier(0.22, 1, 0.36, 1)`
- 新增遮罩 `.nav-mask`（点按关闭）与汉堡按钮 `.nav-toggle`（宽屏 `display: none`）
- `navOpen` 随路由变化自动收起，避免切页后遮挡内容
- `height: 100vh` 之后追加 `height: 100dvh`（不支持的浏览器自动忽略）；内容区底部加 `env(safe-area-inset-bottom)`
- 移动端隐藏「管理员」标签与用户名（`.desktop-only`），退出按钮缩小
- **宽屏分支未改动**：新增样式全部位于 `@media (max-width: 767px)` 内，或默认 `display: none`

**产物级验证**

| 检查 | 结果 |
| :--- | :--- |
| `npm run build`（pc-admin） | 通过（退出码 0） |
| 线上 `/admin/` | 200 |
| 线上 CSS `index-C8Z-iMFB.css` | 含 `nav-toggle` / `nav-mask` / `767px` |
| 线上 JS `index-DlPLALJ3.js` | 含 `nav-mask` / `mobile-open` |
| `scripts/check-entry.ps1 -CheckWs` | 15/15 通过（无回归） |

**待人工确认（真机）**：手机竖屏打开 `/admin/orders`，确认 ① 无横向滚动 ② 汉堡按钮可开合 ③ 顶栏标题不溢出。

## 5. 验收标准（V9.7 整体）

- 360×800 与 390×844：无横向滚动；抽屉可开合；表格页可完成一次操作（改派 / 审批）
- 1024×800、1440×900：与改造前一致
- 触控目标 ≥44px；正文字号 ≥13px
- 手机可经局域网或 cpolar 公网地址访问 `/admin/` 并登录

## 6. 风险与注意

- `el-table` 在移动端体验差：V9.7.2 卡片化会改动较多模板，但只动前端、不动接口
- 区域概览 / 3D 是大画布交互，移动端只读可避免误操作与手势冲突
- 抽屉为手写实现（未用 `el-drawer`），避免 Element 内部样式与 scoped 样式的冲突；键盘焦点管理后续可补

## 7. V9.7.2 实施记录（工单总览，进行中）

新增 `frontend/pc-admin/src/composables/useViewport.ts`（原生 `matchMedia` 侦听，初始值在 setup 阶段同步求值，避免手机端先渲染宽屏结构再切换）；改造 `views/HomeView.vue`（+152 / −1）：

- 表格加 `v-if="!isMobile"`；≤767px 改为卡片列表 `.order-cards`：标题 + 状态徽章 → 标记标签（待管理员处置 / 外援 / 已锁定）→ 字段区（位置 / 类型 / 工人 / 报修人 / 评分，评分保留 tooltip 明细）→ 待派单原因 → 操作区
- 操作区复刻表格全部按钮与显示条件：详情 / 完工 / 锁定解锁 / 优先级 / 手动派单改派 / 标记外援（含 `externalMarking` 加载态），**业务逻辑未改动**
- 宽屏分支零改动：卡片块用 `v-else`，仅窄屏渲染

**产物级验证**

| 检查 | 结果 |
| :--- | :--- |
| `npm run build`（pc-admin） | 通过 |
| 线上 `/admin/` | 200 |
| 线上 JS `index-2KlDhles.js` | 含 `order-cards` / `oc-meta` |
| `scripts/check-entry.ps1 -CheckWs` | 15/15 通过（无回归） |

**待人工确认（真机）**：手机竖屏打开 `/orders`，确认卡片字段完整、操作按钮可点（建议先试「详情」）。

**下一步**：请假审批（`LeavesView.vue`）、报修记录（`RepairRecordsView.vue`）、人员账号（`UsersView.vue`）按同一模式卡片化。

## 8. V9.7.3 移动端弹窗 / 看板 / 规则页修复（2026-09-20，用户实测反馈驱动）

**问题与根因（代码实测）**

| 现象 | 根因 |
| :--- | :--- |
| 工单「详情」只能看到一部分 | 详情是 `el-drawer size="480px"`，手机视口仅 360–390px，抽屉宽于屏幕 |
| 看板卡片被挤成竖条、留大片空白 | `.status-grid` 5 列 / `.history-grid` 3 列 / `.sla-grid` 4 列，窄屏每列仅 60–70px，中文标签逐字换行 |
| 近 7 日趋势与楼栋工单分布挤在一行 | `.grid-2 { grid-template-columns: 1fr 1fr }` 未做窄屏降级 |
| 派单规则配置溢出、控件显示不全 | `.rule-row { grid-template-columns: minmax(0,1fr) 300px 96px 150px }`，固定列宽合计 546px |
| 各页新增/编辑弹窗在手机上溢出 | 弹窗内联 `width="420px" ~ "1200px"`，表单 `label-width="110px"` 吃掉近三分之一直宽 |

**修复**

1. 新增全局适配层 `src/styles/mobile.css`（`main.ts` 在 `theme-v4.css` 之后引入；全部规则位于 `@media (max-width: 767px)` 内，宽屏零影响）：
   - `el-dialog` 内联宽度一律让位于视口：`width: calc(100vw - 20px) !important`；body `max-height: calc(100dvh - 168px)` 可滚动
   - `el-drawer { max-width: 100vw !important }`
   - 表单 `label-width` 固定值在窄屏改上下结构（`float:none; width:auto; text-align:left`，`content` 取消左边距）
   - 输入类控件 `max-width:100%`；表格字号 12px、单元格内边距收紧；分页允许多行；长文本允许断行
2. `views/HomeView.vue`：详情抽屉改 `:size="isMobile ? '100%' : '480px'"`
3. `views/StatsView.vue`：`history-grid / status-grid / sla-grid / eval-grid` 窄屏 2 列；`.grid-2` 改单列（两张图上下错开）；卡片内边距与数字/标签字号下调
4. `views/DispatchRulesView.vue`：`.rule-row` 窄屏单列；`.rule-control` 换行且控件占满宽度

**产物级验证**

| 检查 | 结果 |
| :--- | :--- |
| `npm run build`（pc-admin） | 通过 |
| 线上 `/admin/` | 200 |
| 线上 CSS `index-Wt7YDt2A.css` | 含 `calc(100vw - 20px)` 与 `rule-row` 覆盖 |
| `scripts/check-entry.ps1 -CheckWs` | 15/15 通过（无回归） |

**待真机确认**：`/orders` 详情抽屉、`/stats` 卡片与两张图、`/dispatch-rules` 规则行。

**尚未逐页核对（下一批）**：区域概览（三列画布 210px/1fr/330px）、建筑可视化（3D 与 1200px 弹窗）、排班、人员账号、请假审批、报修记录、建筑信息——全局适配层已覆盖其弹窗/表单溢出，页面级栅格需逐页确认。

## 9. V9.7.2 剩余三页卡片化（2026-09-20）

| 页面 | 数据源 | 卡片字段 | 操作 |
| :--- | :--- | :--- | :--- |
| 请假审批 `LeavesView.vue` | `list` | 工人 / 请假区间 / 原因 / 审批意见 / 提交时间 | 通过 / 驳回 / 撤销（条件与表格一致） |
| 报修记录 `RepairRecordsView.vue` | `records` | 楼栋+房间 / 报修时间 / 故障类型 / 故障描述 / 来源 / 处理工人 / 完工时间 | 详情（打开原有 el-drawer） |
| 人员账号 `UsersView.vue` | `list` | 账号 / 角色 / 工种 / 手机 / 最大并发 / 绑定 | 编辑 / 重置密码 / 停用·启用（条件与表格一致） |

实现方式与工单总览一致：`el-table` 加 `v-if="!isMobile"`，紧随其后渲染 `v-else` 卡片列表；三页共用同一套卡片样式类（`.m-cards / .m-card / .m-head / .m-meta / .m-actions`）。字段表达式直接从原表格列定义提取，避免出现空字段。

**产物级验证**：`npm run build` 通过；产物 `index-yYgSuOdV.js` 含 `m-cards`；线上 `/admin/` 200；`check-entry.ps1 -CheckWs` 15/15。

**前端剩余**：区域概览与 3D 在移动端的只读约束（隐藏编辑入口 + 提示「建议电脑端编辑」）尚未落地。