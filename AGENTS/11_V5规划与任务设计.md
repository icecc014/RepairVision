# 11_V5规划与任务设计.md（2026-09-15 拟定）

> 版本定位：**V5「空间感知的智能派单与排班优化」**
> 本文档先给出总任务与分阶段阶梯计划，再给出关键技术设计，作为 V5 各阶段开工与验收的依据。
> 需求来源：用户 2026-09-15 提出的六部分需求（区域概览、排班与工时、动态派单与路径规划、任务均衡与自动派单、整体目标、其他）。

## 1. 总任务

在 V4 已完成的"工单闭环 + 楼层二三维可视化 + 请假感知排班"基础上，新增**校园/建筑群级别的空间数据（区域概览）**，并据此升级排班与派单：

1. **空间基础**：把现有楼层布局设计器泛化为通用画布，用于绘制校园/园区总平面图（建筑、道路、广场等图元），为派单提供建筑坐标与路网连通关系；
2. **时间基础**：排班改为**双休 + 仅白班**（上午、下午两个工作时段可配置），派单与工作时段解耦，非工作时段工单进入队列；
3. **人员基础**：工人分为**电工与水工**两类，宿管报修时按"电维修 / 水维修"选择，其他类型不自动派单，转由管理员协商或外援；
4. **算法升级**：动态派单以**当前在岗工人**为候选集，结合**路网距离、工种匹配、动态负载均衡**计算代价，批量场景用匈牙利算法求最小总代价，并支持事件驱动的自动重算与积压告警；
5. **管理可干预**：管理端可查看自动派单结果、手动转派、调整优先级与锁定分配，异常情况可控。

**最终目标**：减少无效移动与跨区往返、避免工单积压与分配不均、降低管理端实时干预成本、提升维修效率与工人对建筑群布局的认识。

## 2. 现状盘点（V4 结束时的既有能力）

| 能力 | 现状 | V5 需要 |
| --- | --- | --- |
| 楼层布局编辑器 | 已有 `LayoutDesigner.vue`（整数格区块、跨格、碰撞检测、撤销栈、自定义区域），数据存 `map_db.buildings.layout_json` | 泛化为通用画布，支持校园级图元与更大网格 |
| 空间数据 | 仅楼栋有坐标（pos_x/pos_y）与层内布局，**无路网、无建筑群平面图** | 新增区域概览数据模型与存储 |
| 派单 | 技能 40% + 距离 30%（按楼栋坐标） + 负载 30%，批量用匈牙利算法 | 距离改为**路网最短路**；候选集改为**在岗工人**；加入工种匹配与动态负载均衡 |
| 排班 | 支持 0~3 天/周休息（默认 1），班次含 DAY/MORNING/AFTERNOON/OFF | 改为**双休（默认 2 天，可配置具体休息日）+ 仅白班**；新增工作时段配置与在岗判断 |
| 请假 | 已实现申请、审批、代录、撤销，并参与排班与派单 | 沿用，纳入"在岗"判断 |
| 工种 | 有技能表（`worker_skills`）与故障类型字典 | 新增**电工/水工**分类与故障类型分类、其他类型不自动派单 |
| 告警 | 排班覆盖告警 | 新增**派单积压/异常告警** |

## 3. 需求梳理与调整建议

| 用户需求 | 处理 | 说明 |
| --- | --- | --- |
| 复用绘图能力画校园图 | ✅ 采纳 | 把 `LayoutDesigner` 的网格模型、碰撞检测、撤销栈抽成通用画布，楼层与校园两处复用 |
| 图元：建筑/道路/自定义 | ✅ 采纳并扩展 | 增加"广场/绿地""校门/出入口"两类常用图元；建筑图元可关联已建楼栋或自定义名称 |
| 建筑名称两种来源 | ✅ 采纳 | 关联楼栋（取建筑信息管理中的数据）或自定义输入（食堂、医院、体育馆等） |
| 三端展示与权限 | ✅ 采纳 | 管理端可编辑；宿管端、工人端只读查看 |
| 与派单结合（坐标/道路/距离） | ✅ 采纳 | 建筑→最近道路节点，路网按格加权求最短路，形成建筑间距离矩阵并缓存 |
| 双休制 | ✅ 采纳 | 每周休息天数默认 **2**（可配置 0~3），支持"固定周末"与"错峰轮休"两种模式 |
| 仅白班、无夜班午班 | ✅ 采纳 | 班次简化为 **白班 / 休息**；白班内部按时段描述（上午、下午），时段可配置 |
| 派单与工时解耦 | ✅ 采纳 | 工单可 7×24 生成与分配；工人开工时间受时段约束，非工作时段工单进入"待开工队列" |
| 派单基于在岗状态 | ✅ 采纳 | 在岗 = 今日白班 ∧ 当前处于工作时段 ∧ 未请假 ∧ 账号启用 |
| 动态派单（实时计算） | ✅ 采纳 | 事件驱动（新工单/完工/上下班/请假/调班/手动调整）触发重算，带防抖 |
| 优先同楼栋/相邻楼栋 | ✅ 采纳 | 路网距离本身即可体现；对同楼栋加固定加成分 |
| 优先紧急/超时/等待久 | ✅ 采纳 | 结合已有工单 `priority`，新增"等待时长"因子，超时工单插队 |
| 路径规划结合 | ⚠️ 调整 | V5 先实现**路网最短路距离**（栅格 Dijkstra）；"给工人导航路径"作为 V5.5 之后的扩展 |
| 不设固定任务数上限 | ✅ 采纳 | 取消"达到并发上限即停派"，改为**负载惩罚权重**（软约束），仅保留"同一时刻在手工单数"作为参考展示 |
| 动态负载均衡 | ✅ 采纳 | 人均负载 = 待派工单数 / 在岗人数；对高于均值 1.2 倍的工人降权，批量指派用匈牙利算法最小化总代价 |
| 自动派单 + 管理端可干预 | ✅ 采纳 | 自动派单常驻；管理端支持手动转派、改优先级、锁定某单/某人不参与自动调整 |
| 匈牙利算法 | ✅ 采纳 | 沿用并升级：代价 = 1 − 综合评分 + 负载偏离惩罚 |
| 工人分电工/水工 | ✅ 采纳 | 用户表新增工种字段；派单按故障类型分类匹配 |
| 其他类型暂不派单 | ✅ 采纳 | 非电非水故障 → 自动派单跳过，工单标记"待管理员处置"，管理端提示协商或外援 |

## 4. 分阶段阶梯任务

### V5.1 区域概览（校园平面图）— 空间数据基础
**目标**：管理端能绘制并保存校园/建筑群总平面图，三端可查看。

功能点：
1. 通用画布抽象：从 `LayoutDesigner.vue` 抽出网格模型、碰撞检测、撤销栈、属性面板逻辑，形成可复用组件（楼层布局与区域概览共用）；
2. 图元类型：建筑、道路、广场/绿地、校门/出入口、自定义（可命名）；每种图元有独立配色与图标；
3. 建筑图元两种来源：关联"建筑信息管理"中的楼栋（带出名称与编号）或自定义名称（食堂、医院、体育馆等）；
4. 画布规模：支持更大的网格（默认 40×30，可调至 60×40）与缩放、平移；
5. 存储：`map_db` 新增 `campus_layouts` 表（支持多张图，默认图唯一）；
6. 展示：管理端"区域概览"页（编辑 + 预览）；宿管端与工人端新增只读"校园概览"视图；
7. 保存校验：建筑图元必须命名、道路图元需连通（至少与其他图元接壤），无重叠。

数据与接口：`GET/POST/PUT /api/admin/campus-layout`；`GET /api/worker/campus-map`（宿管与工人共用的只读接口）。
验收：绘制含 4 栋宿舍楼 + 食堂 + 2 条道路的平面图并保存；三端均能正确显示；刷新后不丢失。

### V5.2 工种分类与报修类型治理
**目标**：明确"谁修什么"，其他类型不自动派单。

功能点：
1. 用户表新增 `job_type`（1=电工，2=水工，0=通用）；人员管理中可设置；
2. 故障类型字典新增 `category`（electric / water / other）与 `auto_dispatch`（是否自动派单）；
3. 宿管报修界面：故障类型按"电维修 / 水维修 / 其他"分组，选择后自动带出类别；
4. 派单时按类别筛选候选工人（电工修电、水工修水，通用工人可接两类）；
5. 其他类型工单：不进入自动派单，状态标记"待管理员处置"，管理端列表高亮并提示"协商派单 / 寻求外援"；
6. 管理端可手动物色电工/水工或外部人员完成任务并记录。

数据与接口：`worker_db.users` 增字段；`order_db.fault_types` 增两字段；工单新增"待处置"标记字段或复用 `pending_reason`；管理端新增"待处置工单"筛选。
验收：宿管提交"电维修"工单只派给电工；提交"其他"类型工单不自动派单并出现在管理端待处置列表；管理员可手动指派完成。

### V5.3 双休白班排班改造
**目标**：排班符合"双休 + 仅白班"的实际作息，并提供在岗判断。

功能点：
1. 班次类型简化为"白班 / 休息"（历史午/晚班数据保留但界面不再提供）；
2. 每周休息天数默认 2，支持 0~3；支持两种模式：固定休息日（如周六、周日）与错峰轮休（按楼栋保障代价函数择优，沿用 V4 算法）；
3. 工作时段配置：上午上班/下班、下午上班/下班（默认 08:00–12:00、14:00–18:00），可在系统设置中修改；
4. 在岗判断接口：给定时间点与工人，返回是否在岗（今日白班 ∧ 时段内 ∧ 未请假 ∧ 启用）；
5. 排班页展示：每日在岗人数、休息人数、请假人数，以及"当前在岗 N 人"的实时提示；
6. 派单解耦：非工作时段仍可生成与分配工单，工人开工动作受时段约束（非时段内开工给出提示或拒绝，可配置）。

数据与接口：`worker_db.worker_schedules` 班次取值收敛；新增系统配置表或沿用派单规则表存工作时段；`GET /api/worker/duty-status`。
验收：生成排班后每人每周恰好休 2 天；界面只出现"白班/休息"；非工作时段派单成功但工单处于"待开工"；上班后按在岗工人重算派单。

### V5.4 空间感知的动态派单与负载均衡（核心）
**目标**：以在岗工人为候选集，结合路网距离与动态负载，自动、均衡地派单。

功能点：
1. 路网距离：由区域概览的道路图元构建栅格图，建筑图元接入最近道路节点；用 Dijkstra 计算建筑间最短路，缓存 N×N 距离矩阵（布局变更时失效重建）；
2. 候选集：在岗 ∧ 工种匹配 ∧ 账号启用的工人；
3. 评分：技能匹配 40% + 路网距离 30% + 负载 30%（权重可配置）；同楼栋加成、跨区惩罚；
4. 优先级：紧急/超时/等待时长长的工单优先（等待时长因子），超时工单插队；
5. 动态负载均衡：人均负载 = 待派工单数 / 在岗人数；对负载 > 人均×1.2 的工人施加惩罚权重；批量指派用匈牙利算法最小化总代价（代价 = 1 − 评分 + 负载偏离惩罚）；
6. 取消硬性并发上限：不再因"达到上限"而停止派单，改为权重惩罚与告警；
7. 事件驱动重算：新工单创建、工单完工、上下班切换、请假生效/撤销、临时调班、手动调整 → 触发重算（防抖 1~3 秒）；
8. 积压告警：待派工单数超过"在岗人数 × 阈值"或存在超过 X 小时未派出的工单 → 管理端告警并给出建议（增援/调整排班/外援/手动干预）；
9. 管理端干预：派单结果面板，支持手动转派、调整优先级、锁定（某单不自动改派 / 某工人不参与本轮调整）。

数据与接口：`order_db` 新增执行计划字段（预计到达时间等，可选）；新增 `GET /api/admin/dispatch-plan`、`POST /api/admin/dispatch/recalc`、`POST /api/admin/orders/:id/lock`。
验收：构造"4 名工人（2 电 2 水）+ 8 张工单 + 路网"的场景，验证：只派给在岗且工种匹配的工人；批量派单总距离明显小于逐单贪心；负载分布标准差下降；超时工单被优先处理；新工单加入后自动重算。

### V5.5 管理端干预完善、回归与收尾
**目标**：异常可控、全量回归、文档与论文素材同步。

功能点：
1. 派单结果看板：按工人展示在途工单、预计工时、负载偏离度、当前所在楼栋（来自最近完成工单）；
2. 异常处理：待处置工单（其他类型）、超时未派、无可用工人等场景的人工处理入口；
3. 区域概览与派单联动演示：修改路网后距离矩阵重建并影响派单；
4. 回归：三端接口冒烟、算法单测（距离矩阵、评分、匈牙利、负载均衡、在岗判断）、前后端构建与静态检查；
5. 文档：更新 V5 完成记录、论文算法素材（空间感知派单的建模与实验设计）。

验收：全套回归通过，形成"区域概览 → 在岗判断 → 路网距离 → 负载均衡 → 自动派单 → 管理干预"的完整演示动线。

## 5. 关键技术设计

### 5.1 区域概览数据模型（campus layout）

```json
{
  "version": 1,
  "cols": 40, "rows": 30,
  "blocks": [
    { "id": "b1", "kind": "building", "row": 4,  "col": 6,  "rowSpan": 4, "colSpan": 6,
      "buildingId": 1, "label": "1号宿舍楼" },
    { "id": "b2", "kind": "building", "row": 4,  "col": 22, "rowSpan": 4, "colSpan": 6,
      "label": "第二食堂", "customBuilding": true },
    { "id": "r1", "kind": "road",     "row": 12, "col": 0,  "rowSpan": 1, "colSpan": 40 },
    { "id": "g1", "kind": "gate",     "row": 0,  "col": 18, "rowSpan": 1, "colSpan": 3, "label": "南门" }
  ]
}
```

- 图元 `kind`：`building`（建筑）/ `road`（道路）/ `green`（广场绿地）/ `gate`（校门）/ `custom`（自定义）；
- 复用 V4 的整数格模型、逐格试探碰撞检测与撤销栈；网格上限提高到 60×40；
- 存储：`map_db.campus_layouts(id, name, is_default, cols, rows, layout_json, updated_at)`，默认图唯一；由 map 服务通过 RPC 暴露给 order 服务与前端。

### 5.2 路网模型与距离计算

1. **建图**：道路格 → 栅格节点（4 邻接），边权 = 1 格；建筑图元找出其边界上"与道路格 4 邻接"的格作为**入口节点**（多个入口取最短者）；
2. **最短路**：以每栋建筑的入口为源点跑一次 Dijkstra，得到建筑间最短路（格数）；距离（米）= 格数 × 格边长（默认 10 米，可配置）；
3. **缓存**：N 栋建筑的距离矩阵（N ≤ 60），布局保存后重建并落 Redis 缓存；
4. **复杂度**：O(N × R log R)，R 为道路格数（≤ 2400），单次重建 < 50ms；
5. 未接入道路的建筑（孤立图元）：退化为欧氏距离并给出提示。

### 5.3 派单算法升级（V5.4 核心）

```
候选集 C = { w | 在岗(w) ∧ 工种匹配(w, order) ∧ 账号启用(w) }
人均负载 avg = 待派工单数 / |C|
score(w, o) = 0.4×技能 + 0.3×(1 − 距离归一化) + 0.3×(1 − 负载偏离)
负载偏离(w) = max(0, 负载(w) − avg) / max(1, avg)      # 高于均值才有惩罚
同楼栋加成 = +0.1（同栋） / +0.05（相邻楼栋且路网距离 < 2 格）
紧急度 = 超时 +0.15；等待时长每超 30 分钟 +0.05（上限 +0.3）
cost(w, o) = 1 − clamp(score) + 0.5×负载偏离(w)
批量：min Σ cost(w, o) s.t. 匈牙利算法一对一指派（无硬性并发上限）
```

- 事件驱动：`new_order` / `order_done` / `duty_change` / `leave_change` / `manual_adjust` 触发重算，1~3 秒防抖；
- 自动派单常驻：定时（如 60 秒）兜底扫描，处理漏派与积压；
- 告警阈值：待派 > 在岗人数 × 3，或存在等待 > 2 小时未派出的工单。

### 5.4 排班改造（V5.3）

- 班次：`DAY`（白班）/ `OFF`（休息）；工作时段由配置项 `work_morning(08:00-12:00)`、`work_afternoon(14:00-18:00)` 描述；
- 双休：`rest_days_per_week = 2`（默认），模式 `fixed`（固定周六周日）或 `staggered`（错峰，沿用 V4 代价函数，加入"楼栋在岗保障"）；
- 在岗判断：`is_on_duty(worker, ts) = 今日排班=DAY ∧ ts ∈ 任一工作时段 ∧ 当日无已批准请假 ∧ 账号启用`；
- 与派单解耦：派单不受时段限制；`开工` 动作受时段约束（默认提示但允许强制开工，可配置为拒绝）。

### 5.5 工种与其他类型处置（V5.2）

- `worker_db.users.job_type`：1=电工，2=水工，0=通用；
- `order_db.fault_types.category`：electric / water / other；`auto_dispatch`：1/0；
- 宿管报修页按"电维修 / 水维修 / 其他"分组选择；
- 其他类型：`auto_dispatch = 0` → 工单进入"待管理员处置"，不进入自动派单队列，管理端提供"协商派单 / 标记外援 / 手动指派"三个动作。

## 6. 数据库迁移清单（V5）

| 库 | 变更 | 阶段 |
| --- | --- | --- |
| map_db | 新增 `campus_layouts` 表 | V5.1 |
| worker_db | `users` 增 `job_type`；`worker_schedules.shift_type` 取值收敛为 DAY/OFF | V5.2 / V5.3 |
| order_db | `fault_types` 增 `category`、`auto_dispatch`；`orders` 增 `dispatch_state`（pending/manual/queued）或复用 `pending_reason` | V5.2 / V5.4 |
| 配置 | 新增系统配置项：工作时段、休息日模式、负载阈值、告警阈值（可存 `dispatch_rule_config` 扩展键） | V5.3 / V5.4 |

## 7. 前端改动清单

| 端 | 页面 | 改动 |
| --- | --- | --- |
| PC | 区域概览（新增） | 通用画布编辑器：图元工具栏、属性面板、缩放平移、保存与预览 |
| PC | 建筑信息管理 | 建筑图元可从本页数据选择（关联建筑） |
| PC | 调度看板 / 工单总览 | 增加"在岗工人"、"负载偏离度"、"待处置工单"、自动派单结果与手动干预入口 |
| PC | 工人排班 | 双休模式配置、白班/休息展示、当前在岗人数、工作时段设置 |
| PC | 人员账号管理 | 新增工种（电工/水工/通用）设置 |
| PC | 维修类型字典 | 新增类别（电/水/其他）与"是否自动派单"开关 |
| H5 | 宿管报修 | 故障类型按电/水/其他分组选择 |
| H5 | 宿管 / 工人 | 新增只读"校园概览"视图（复用同一份区域概览数据） |
| H5 | 工人地图 | 楼栋总览与区域概览叠加，显示当前所在楼栋与在途工单位置 |

## 8. 待确认的默认决策

| 项 | 默认方案 | 备选 |
| --- | --- | --- |
| 双休模式 | 默认**错峰轮休**（保证每栋楼在岗），管理端可改为固定周末 | 固定周六周日为默认 |
| 工作时段 | 上午 08:00–12:00、下午 14:00–18:00，可配置 | 其他时段 |
| 非工作时段开工 | 提示"非工作时段"，允许强制开工（可配置为禁止） | 直接禁止开工 |
| 其他类型工单 | `auto_dispatch = 0`，进入"待管理员处置" | 首页提示外援电话 |
| 区域概览张数 | 默认 1 张（可扩展多张，如东/西校区） | 只允许 1 张 |
| 距离计算粒度 | 栅格最短路（格数 × 格边长，默认 10 米/格） | 直接按坐标欧氏距离 |
| 并发上限 | 取消硬上限，改为负载惩罚 + 告警 | 保留上限但可配置 |

## 9. 风险与兼容性

- **数据兼容**：V4 的楼层布局数据（layout_json v2）保持不变；校园概览使用新表，互不影响；
- **班次兼容**：历史排班中的 MORNING / AFTERNOON 记录保留展示为"白班（历史）"，新生成排班只写 DAY/OFF；
- **派单兼容**：无区域概览数据时，距离因子退化为楼栋坐标欧氏距离，保证功能可用；
- **性能**：距离矩阵缓存 + 防抖重算，避免高频事件导致重复计算；批量指派规模上限（工单 ≤ 100、工人 ≤ 50）保证毫秒级返回；
- **可解释性**：派单结果需展示各因子得分与最终代价，便于答辩演示与问题排查。

## 10. 决策确认与调整（2026-09-15）

用户已确认以下默认方案：

1. **双休模式**：采用**错峰轮休**（保证每栋楼在岗），管理端仍可改为固定周末；
2. **白班时段**：默认 08:00–12:00、14:00–18:00，可配置；
3. **非工作时段开工**：**允许强制开工**（给出提示但不阻塞）；
4. **其他类型工单**：`auto_dispatch = 0`，进入"待管理员处置"，管理端提供协商派单 / 标记外援 / 手动指派；
5. **并发上限**：取消硬性并发上限，改为**负载惩罚 + 积压告警**。

### 10.1 双阈值保护机制（用户新增要求）

在取消硬上限的同时，保留两条可配置阈值形成"预警—保护"两级机制：

| 阈值 | 触发条件（可配置） | 系统行为 |
| --- | --- | --- |
| **预警线** | 待派工单数 > 在岗人数 × 3，或存在等待超过 2 小时未派出的工单 | 管理端高亮提醒（调度看板 + 工单总览角标），不改变自动派单行为 |
| **保护线** | 待派工单数 > 在岗人数 × 5，或存在等待超过 4 小时未派出的工单 | **暂停自动派单**，切换为"人工处置模式"：新工单只入队不自动分配，管理端收到高优先级告警与处理建议（增援 / 调班 / 外援 / 手动指派），处理完毕后点击"恢复自动派单" |

- 两条阈值均可在"派单规则配置"页调整（默认 3 和 5），保护线必须大于预警线；
- 进入保护模式与退出保护模式都会写入操作日志，并在管理端顶部展示当前模式（自动 / 人工）；
- 保护模式下仍允许管理端手动派单与改派，工人端不受影响。

### 10.2 实施口径调整（区域概览存储位置）

为降低 V5.1 的跨服务改动成本，区域概览数据先由 **order 服务托管在 `order_db.campus_layouts`**（对外仍提供 `/api/admin/campus-layout` 与 `/api/worker/campus-map`），布局 JSON 内通过 `buildingId` 关联楼栋、并保留 `label` 冗余名称：

- 优点：无需改动 map/worker 服务的 proto 与 RPC，可最快让三端看到效果；
- 后续如需迁移到 map 服务，只需把 store 层实现替换为 map RPC 调用，接口与数据模型不变（表结构与 JSON 格式保持一致即可平滑迁移）。

## 11. V5.1 进度记录（2026-09-15，进行中）

### 已完成

- **数据层**：新增 `order_db.campus_layouts` 表（`grid_cols` / `grid_rows` / `layout_json`，迁移脚本 `db/migrations/20260915_campus_layout.sql`，同时写入 `db/init/01_schema.sql`），并已应用到运行库（默认行 id=1，40×30）。
  - 注：`rows` 是 MySQL 8 保留字，列名改用 `grid_cols` / `grid_rows`，对外 JSON 字段仍为 `cols` / `rows`。
- **后端**：order 服务新增 `store/campuslayout.go`、`types/campuslayout.go`、`logic/campuslayoutlogic.go`、`handler/campuslayouthandler.go`：
  - `GET /api/admin/campus-layout`（管理员查询，不存在时自动创建空白图）、
  - `POST /api/admin/campus-layout`（管理员保存，列数上限 80、行数上限 60）、
  - `GET /api/campus-map`（宿管端 / 工人端只读，任何已登录角色可访问）。
  - 回归：管理端保存 4 个图元（2 建筑 + 1 道路 + 1 校门，401 字节）后，工人端 `GET /api/campus-map` 读到同一份数据。
- **前端（PC）**：新增 `views/CampusOverviewView.vue` 区域概览编辑页 + `utils/campusLayout.ts` 图元模型（复用楼层布局的整数格几何算法与类型转换）：
  - 5 类图元（建筑 / 道路 / 广场绿地 / 校门 / 自定义）+ 擦除；
  - 建筑图元支持"关联已有楼栋（下拉选择建筑信息管理中的楼栋）"或"自定义命名"；
  - 属性面板支持类型、名称、起始格、跨格数、四边扩收、删除；
  - 交互沿用 V4 设计器：单击放置、双击进入编辑模式（8 个把手）、拖动改尺寸、整体移动、Ctrl+Z 撤销 / Delete 删除 / Esc 退出；
  - 一键"生成示例布局"（主干道 + 支路 + 5 栋建筑 + 广场 + 校门）、画布尺寸调整、保存与放弃修改；
  - 侧边栏新增「区域概览」菜单，路由 `/campus`。
- 构建与部署：`go build`（order）、`node scripts/check-sfc-bindings.mjs`、PC `npm run build` 均通过；order-api 与 web 已重建，线上包已包含区域概览页面。

### 待完成（V5.1 剩余）

1. 宿管端与工人端的**只读区域概览视图**（复用 `GET /api/campus-map`，H5 端新增校园总览页或在楼栋地图页加切换）；
2. 建筑图元与楼栋的双向一致性（楼栋改名后在概览中同步提示）；
3. 概览与派单的联动（V5.4 实现：由道路图元构建路网并计算建筑间距离矩阵）。

### V5.1 完成记录（2026-09-15）

在第 11 节基础上补齐 H5 只读视图，V5.1 全部完成：

- **H5 组件** `frontend/mobile-web/src/components/CampusOverviewMap.vue`：只读渲染区域概览（SVG，按 `cols/rows` 网格等比缩放），支持 5 类图元配色与图例、建筑名称标签、按建筑高亮描边；
- **接口**：H5 新增 `apiCampusMap()` → `GET /api/campus-map`（任何已登录角色可读，宿管与工人共用）；
- **工人端**：`MapView.vue` 顶部新增「校园概览」卡片（可折叠），并把工人当前选中的楼栋传入组件做高亮，帮助工人理解"我在哪、其它楼栋在哪"；
- **宿管端**：`DormView.vue` 在统计区下方新增同样的「校园概览」卡片（可折叠）；
- **建筑名称同步**：组件渲染建筑图元时，**优先使用"建筑信息管理"中的最新楼栋名称**（按 `buildingId` 匹配），其次才用概览保存时的名称——楼栋改名后三端显示自动跟随；
- 校验：`node scripts/check-sfc-bindings.mjs`（两个前端均无未声明引用）、H5 `npm run build` 通过；web 容器已重建，线上 H5 包已包含「校园概览」与空态提示。

至此 V5.1 验收项均已完成：
1. 管理端可自由绘制、编辑、保存区域概览 ✔（含建筑关联楼栋 / 自定义命名、5 类图元、示例布局、缩放网格）
2. 宿管端、工人端可查看区域概览 ✔（只读、可折叠、建筑高亮）
3. 数据可供后续派单使用 ✔（`GET /api/campus-map` 输出建筑坐标与道路图元，V5.4 将据此构建路网与距离矩阵）

## 12. V5.2 进度记录（2026-09-15，数据层与协议层完成）

### 已完成

1. **数据库变更**（迁移 `db/migrations/20260915_v5_2_jobtype.sql`，已应用到运行库）：
   - `worker_db.users` 新增 `job_type`（0通用 / 1电工 / 2水工）；
   - `order_db.fault_types` 新增 `category`（electric / water / other）与 `auto_dispatch`（是否参与自动派单）；
   - `order_db.orders` 新增 `manual_review`（1 = 待管理员处置，不自动派单）；
   - 现网故障类型已归类：`electric`、`lamp` → 电（自动派单）；`water` → 水（自动派单）；`other` → 其他（**不自动派单**）；
   - 演示数据：`worker1` 设为电工、`worker2` 设为水工，其余工人保持"通用"（两类都能接）。
2. **协议层**：`services/worker/worker.proto` 的 `User`、`CreateUserRequest`、`UpdateUserRequest` 三个消息新增 `int64 job_type`，已用 protoc 23.1 重新生成 `worker.pb.go`（新增 33 行，类型字段名 `JobType`）；`go build`（worker / order）通过。
   - 备注：protoc 工具已固定放在项目 `.tools/protoc-23.1/`（该目录被 git 忽略），避免再次丢失。

### 待完成（V5.2 剩余）

1. **worker 服务**：`store/user.go` 与用户相关 logic 读写 `job_type`（列表、创建、更新）；
2. **order 服务**：
   - `store/faulttype.go` 读取 `category` / `auto_dispatch`，管理端新增与编辑故障类型时写入；
   - `store/order.go` 的 `Order` 结构与 `InsertOrder` 增加 `manual_review`；
   - `createorderlogic.go`：按故障类型判断，若 `category = other` 或 `auto_dispatch = 0` → 置 `manual_review = 1`、跳过自动派单、写入待处置原因；
   - `dispatch_engine.go`：候选工人按**工种匹配**筛选（电工只接电类、水工只接水类、通用两者皆可），并跳过 `manual_review = 1` 的工单；批量指派沿用匈牙利算法；
   - 接口类型：`FaultType` / `AdminFaultTypeItem` 增加类别与是否自动派单，`OrderItem` 增加"待管理员处置"标记，`AdminUserItem` 增加工种。
3. **前端**：
   - 管理端「人员账号管理」增加工种（电工 / 水工 / 通用）设置；
   - 管理端「维修类型字典」增加类别与"是否自动派单"开关；
   - 管理端「工单总览」对"待管理员处置"工单高亮，并提供协商派单 / 标记外援 / 手动指派入口；
   - 宿管端报修页故障类型按"电维修 / 水维修 / 其他"分组选择。
4. **验收**：宿管提交电类工单只派给电工；提交水类只派给水工；提交"其他"类型不自动派单并出现在管理端待处置列表；管理员可手动指派完成。

### V5.2 第二批：存储层（2026-09-15）

- `order_db.orders.manual_review` 已接入 order 服务存储层：`Order` 结构新增 `ManualReview`、查询列与 `InsertOrder` 同步；**待派队列（`ListPendingOrders` / `ListPendingOrdersByBuilding`）已排除 `manual_review = 1` 的工单**，即"待管理员处置"的工单不会被自动派单引擎取走；
- `fault_types` 的 `category` / `auto_dispatch` 已接入故障类型存储层（列表 / 按编码 / 按 ID 查询均返回两个新字段，缺省分别回退为 `other` 与 1）；
- 新增 `CreateFaultTypeFull` / `UpdateFaultTypeFull`（含类别与自动派单开关），**保留原有 `CreateFaultType` / `UpdateFaultType` 签名**，因此管理端既有调用与种子数据不受影响，可逐步替换；
- `go build ./...`（order）通过。

剩余（下一步）：
1. 创建工单时按故障类型判定——`category = other` 或 `auto_dispatch = 0` → 置 `manual_review = 1` 并写入待处置原因；
2. 派单引擎候选工人按工种匹配（电工只接电、水工只接水、通用皆可）；
3. 管理端故障类型表单接入类别与自动派单开关；
4. 前端工种设置、工单待处置高亮、宿管报修分类选择；
5. 接口级验收（电→电工、水→水工、其他→待处置）。